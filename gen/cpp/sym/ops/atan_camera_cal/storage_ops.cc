// -----------------------------------------------------------------------------
// This file was autogenerated by symforce from template:
//     cpp_templates/ops/CLASS/storage_ops.cc.jinja
// Do NOT modify by hand.
// -----------------------------------------------------------------------------

#include "./storage_ops.h"

#include <algorithm>
#include <cassert>

#include <Eigen/Dense>

#include <sym/atan_camera_cal.h>

namespace sym {

template <typename ScalarType>
void StorageOps<ATANCameraCal<ScalarType>>::ToStorage(const ATANCameraCal<ScalarType>& a,
                                                      ScalarType* out) {
  assert(out != nullptr);
  std::copy_n(a.Data().data(), a.StorageDim(), out);
}

template <typename ScalarType>
ATANCameraCal<ScalarType> StorageOps<ATANCameraCal<ScalarType>>::FromStorage(
    const ScalarType* data) {
  assert(data != nullptr);
  return ATANCameraCal<ScalarType>(
      Eigen::Map<const typename ATANCameraCal<ScalarType>::DataVec>(data));
}

}  // namespace sym

// Explicit instantiation
template struct sym::StorageOps<sym::ATANCameraCal<double>>;
template struct sym::StorageOps<sym::ATANCameraCal<float>>;
