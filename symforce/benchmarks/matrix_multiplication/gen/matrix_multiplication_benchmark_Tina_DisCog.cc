// -----------------------------------------------------------------------------
// This file was autogenerated by symforce from template:
//     matrix_multiplication_benchmark.cc.jinja
// Do NOT modify by hand.
// -----------------------------------------------------------------------------

///
/// Experiment to show timing for the matrix multiplication example in the SymForce paper.
///
/// Run with:
///
///     build/bin/benchmarks/matrix_multiplication_benchmark_Tina_DisCog
///
/// See run_matmul_benchmarks.py for more information
///

#include <chrono>
#include <thread>

#include <Eigen/Dense>
#include <Eigen/Sparse>
#include <spdlog/spdlog.h>

#include <symforce/opt/tic_toc.h>
#include <symforce/opt/util.h>

#include "catch.hpp"

using namespace sym;

#include "./Tina_DisCog_compute_a.h"
#include "./Tina_DisCog_compute_a_dense.h"
#include "./Tina_DisCog_compute_a_dense_dynamic.h"
#include "./Tina_DisCog_compute_at_b.h"
#include "./Tina_DisCog_compute_b.h"
#include "./Tina_DisCog_compute_b_dense.h"
#include "./Tina_DisCog_compute_b_dense_dynamic.h"

template <typename Scalar>
__attribute__((noinline)) Eigen::Matrix<Scalar, 11, 11> TinaDiscogComputeDenseFixed(
    const Scalar x0, const Scalar x1) {
  Eigen::Matrix<Scalar, 11, 11> A = TinaDiscogComputeADense<Scalar>(x0, x1);
  Eigen::Matrix<Scalar, 11, 11> B = TinaDiscogComputeBDense<Scalar>(x0, x1);
  return A.transpose() * B;
}

template <typename Scalar>
__attribute__((noinline)) Eigen::Matrix<Scalar, Eigen::Dynamic, Eigen::Dynamic>
TinaDiscogComputeDenseDynamic(const Scalar x0, const Scalar x1) {
  const auto A = TinaDiscogComputeADenseDynamic<Scalar>(x0, x1);
  const auto B = TinaDiscogComputeBDenseDynamic<Scalar>(x0, x1);
  return A.transpose() * B;
}

template <typename Scalar>
__attribute__((noinline)) Eigen::SparseMatrix<Scalar> TinaDiscogComputeSparse(const Scalar x0,
                                                                              const Scalar x1) {
  Eigen::SparseMatrix<Scalar> A = TinaDiscogComputeA<Scalar>(x0, x1);
  Eigen::SparseMatrix<Scalar> B = TinaDiscogComputeB<Scalar>(x0, x1);
  return A.transpose() * B;
}

// ----------------------------------------------------------------------------
// Test Cases
// ----------------------------------------------------------------------------

TEMPLATE_TEST_CASE("Tina_DisCog_sparse", "", double, float) {
  using Scalar = TestType;

  std::chrono::milliseconds timespan(100);
  std::this_thread::sleep_for(timespan);

  Scalar sum = 0.0;
  {
    SYM_TIME_SCOPE("Tina_DisCog_sparse_{}", typeid(Scalar).name());

    for (Scalar x0 = 0.1; x0 < 100.0; x0 += 0.1) {
      for (Scalar x1 = 0.1; x1 < 100.0; x1 += 0.1) {
        auto mat = TinaDiscogComputeSparse(x0, x1);
        sum += mat.valuePtr()[0];
      }
    }
  }
}

TEMPLATE_TEST_CASE("Tina_DisCog_dense_dynamic", "", double, float) {
  using Scalar = TestType;

  std::chrono::milliseconds timespan(100);
  std::this_thread::sleep_for(timespan);

  Scalar sum = 0.0;
  {
    SYM_TIME_SCOPE("Tina_DisCog_dense_dynamic_{}", typeid(Scalar).name());
    for (Scalar x0 = 0.1; x0 < 100.0; x0 += 0.1) {
      for (Scalar x1 = 0.1; x1 < 100.0; x1 += 0.1) {
        auto mat = TinaDiscogComputeDenseDynamic<Scalar>(x0, x1);
        sum += mat(0, 0);
      }
    }
  }
}

TEMPLATE_TEST_CASE("Tina_DisCog_dense_fixed", "", double, float) {
  using Scalar = TestType;

  std::chrono::milliseconds timespan(100);
  std::this_thread::sleep_for(timespan);

  Scalar sum = 0.0;
  {
    SYM_TIME_SCOPE("Tina_DisCog_dense_fixed_{}", typeid(Scalar).name());
    for (Scalar x0 = 0.1; x0 < 100.0; x0 += 0.1) {
      for (Scalar x1 = 0.1; x1 < 100.0; x1 += 0.1) {
        auto mat = TinaDiscogComputeDenseFixed<Scalar>(x0, x1);
        sum += mat(0, 0);
      }
    }
  }
}

TEMPLATE_TEST_CASE("Tina_DisCog_flattened", "", double, float) {
  using Scalar = TestType;

  std::chrono::milliseconds timespan(100);
  std::this_thread::sleep_for(timespan);

  Scalar sum = 0.0;
  {
    SYM_TIME_SCOPE("Tina_DisCog_flattened_{}", typeid(Scalar).name());
    for (Scalar x0 = 0.1; x0 < 100.0; x0 += 0.1) {
      for (Scalar x1 = 0.1; x1 < 100.0; x1 += 0.1) {
        auto mat = TinaDiscogComputeAtB(x0, x1);
        sum += mat(0, 0);
      }
    }
  }
}
