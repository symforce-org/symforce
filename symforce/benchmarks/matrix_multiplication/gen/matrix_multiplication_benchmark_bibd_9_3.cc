// -----------------------------------------------------------------------------
// This file was autogenerated by symforce from template:
//     matrix_multiplication_benchmark.cc.jinja
// Do NOT modify by hand.
// -----------------------------------------------------------------------------

///
/// Experiment to show timing for the matrix multiplication example in the SymForce paper.
///
/// Run with:
///
///     build/bin/benchmarks/matrix_multiplication_benchmark_bibd_9_3
///
/// See run_matmul_benchmarks.py for more information
///

#include <chrono>
#include <thread>

#include <Eigen/Dense>
#include <Eigen/Sparse>
#include <spdlog/spdlog.h>

#include <symforce/opt/tic_toc.h>
#include <symforce/opt/util.h>

#include "catch.hpp"

using namespace sym;

#include "./bibd_9_3_compute_a.h"
#include "./bibd_9_3_compute_a_dense.h"
#include "./bibd_9_3_compute_a_dense_dynamic.h"
#include "./bibd_9_3_compute_at_b.h"
#include "./bibd_9_3_compute_b.h"
#include "./bibd_9_3_compute_b_dense.h"
#include "./bibd_9_3_compute_b_dense_dynamic.h"

template <typename Scalar>
__attribute__((noinline)) Eigen::Matrix<Scalar, 84, 84> Bibd93ComputeDenseFixed(const Scalar x0,
                                                                                const Scalar x1) {
  Eigen::Matrix<Scalar, 36, 84> A = Bibd93ComputeADense<Scalar>(x0, x1);
  Eigen::Matrix<Scalar, 36, 84> B = Bibd93ComputeBDense<Scalar>(x0, x1);
  return A.transpose() * B;
}

template <typename Scalar>
__attribute__((noinline)) Eigen::Matrix<Scalar, Eigen::Dynamic, Eigen::Dynamic>
Bibd93ComputeDenseDynamic(const Scalar x0, const Scalar x1) {
  const auto A = Bibd93ComputeADenseDynamic<Scalar>(x0, x1);
  const auto B = Bibd93ComputeBDenseDynamic<Scalar>(x0, x1);
  return A.transpose() * B;
}

template <typename Scalar>
__attribute__((noinline)) Eigen::SparseMatrix<Scalar> Bibd93ComputeSparse(const Scalar x0,
                                                                          const Scalar x1) {
  Eigen::SparseMatrix<Scalar> A = Bibd93ComputeA<Scalar>(x0, x1);
  Eigen::SparseMatrix<Scalar> B = Bibd93ComputeB<Scalar>(x0, x1);
  return A.transpose() * B;
}

// ----------------------------------------------------------------------------
// Test Cases
// ----------------------------------------------------------------------------

TEMPLATE_TEST_CASE("bibd_9_3_sparse", "", double, float) {
  using Scalar = TestType;

  std::chrono::milliseconds timespan(100);
  std::this_thread::sleep_for(timespan);

  Scalar sum = 0.0;
  {
    SYM_TIME_SCOPE("bibd_9_3_sparse_{}", typeid(Scalar).name());

    for (Scalar x0 = 0.1; x0 < 100.0; x0 += 0.1) {
      for (Scalar x1 = 0.1; x1 < 1.0; x1 += 0.1) {
        auto mat = Bibd93ComputeSparse(x0, x1);
        sum += mat.valuePtr()[0];
      }
    }
  }
}

TEMPLATE_TEST_CASE("bibd_9_3_dense_dynamic", "", double, float) {
  using Scalar = TestType;

  std::chrono::milliseconds timespan(100);
  std::this_thread::sleep_for(timespan);

  Scalar sum = 0.0;
  {
    SYM_TIME_SCOPE("bibd_9_3_dense_dynamic_{}", typeid(Scalar).name());
    for (Scalar x0 = 0.1; x0 < 100.0; x0 += 0.1) {
      for (Scalar x1 = 0.1; x1 < 1.0; x1 += 0.1) {
        auto mat = Bibd93ComputeDenseDynamic<Scalar>(x0, x1);
        sum += mat(0, 0);
      }
    }
  }
}

TEMPLATE_TEST_CASE("bibd_9_3_dense_fixed", "", double, float) {
  using Scalar = TestType;

  std::chrono::milliseconds timespan(100);
  std::this_thread::sleep_for(timespan);

  Scalar sum = 0.0;
  {
    SYM_TIME_SCOPE("bibd_9_3_dense_fixed_{}", typeid(Scalar).name());
    for (Scalar x0 = 0.1; x0 < 100.0; x0 += 0.1) {
      for (Scalar x1 = 0.1; x1 < 1.0; x1 += 0.1) {
        auto mat = Bibd93ComputeDenseFixed<Scalar>(x0, x1);
        sum += mat(0, 0);
      }
    }
  }
}

TEMPLATE_TEST_CASE("bibd_9_3_flattened", "", double, float) {
  using Scalar = TestType;

  std::chrono::milliseconds timespan(100);
  std::this_thread::sleep_for(timespan);

  Scalar sum = 0.0;
  {
    SYM_TIME_SCOPE("bibd_9_3_flattened_{}", typeid(Scalar).name());
    for (Scalar x0 = 0.1; x0 < 100.0; x0 += 0.1) {
      for (Scalar x1 = 0.1; x1 < 1.0; x1 += 0.1) {
        auto mat = Bibd93ComputeAtB(x0, x1);
        sum += mat.valuePtr()[0];
      }
    }
  }
}
