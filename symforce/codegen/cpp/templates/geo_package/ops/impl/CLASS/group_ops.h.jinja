{%- import "../../../../util/util.jinja" as util with context -%}

//  ----------------------------------------------------------------------------
// This file was autogenerated by symforce. Do NOT modify by hand.
// -----------------------------------------------------------------------------
#pragma once

#include <Eigen/Dense>

#include <geo/{{ cls.__name__ | lower }}.h>

namespace geo {
namespace {{ cls.__name__ | lower }} {

/**
 * C++ GroupOps implementation for {{ cls }}.
 */
template <typename Scalar>
struct GroupOps {
  {% for spec in specs['GroupOps'] %}
  static {{ util.format_typename(spec.return_type) }} {{ spec.name }}(
    {%- for arg, type in spec.args -%}
    {{ util.format_input_arg(type, arg) }}{% if not loop.last %}, {% endif %}
    {%- endfor -%}
  );
  {% endfor %}

};

}  // namespace {{ cls.__name__ | lower }}

// Wrapper to specialize the public concept

{% for Scalar in scalar_types %}
template <>
struct GroupOps<{{ cls.__name__ }}<{{ Scalar }}>> : public {{ cls.__name__ | lower }}::GroupOps<{{ Scalar }}> {};
{% endfor %}

}  // namespace geo
