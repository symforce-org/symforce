{%- import "../../../util/util.jinja" as util with context -%}

//  ----------------------------------------------------------------------------
// This file was autogenerated by symforce. Do NOT modify by hand.
// -----------------------------------------------------------------------------

#include "./storage_ops.h"

namespace cam {
namespace {{ camelcase_to_snakecase(cls.__name__) }} {

template <typename Scalar>
void StorageOps<Scalar>::ToStorage(const {{ cls.__name__ }}<Scalar>& a, std::vector<Scalar>* vec) {
  assert(vec != nullptr);
  const typename {{ cls.__name__ }}<Scalar>::DataVec& data = a.Data();
  vec->resize(data.size());
  std::copy_n(data.data(), data.size(), vec->begin());
}

template <typename Scalar>
{{ cls.__name__ }}<Scalar> StorageOps<Scalar>::FromStorage(const std::vector<Scalar>& vec) {
  assert(vec.size() == StorageOps<Scalar>::StorageDim());
  return {{ cls.__name__ }}<Scalar>(
    Eigen::Map<const typename {{ cls.__name__ }}<Scalar>::DataVec>(vec.data()));
}

}  // namespace {{ camelcase_to_snakecase(cls.__name__) }}
}  // namespace cam


// Explicit instantiation
{% for Scalar in scalar_types %}
template struct cam::{{ camelcase_to_snakecase(cls.__name__) }}::StorageOps<{{ Scalar }}>;
{% endfor %}
