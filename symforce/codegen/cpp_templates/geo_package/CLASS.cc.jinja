//  -----------------------------------------------------------------------------
// This file was autogenerated by symforce. Do NOT modify by hand.
// -----------------------------------------------------------------------------

#include "./{{ cls.__name__ | lower }}.h"

// Explicit instantiation
{% for scalar in scalar_types %}
template class geo::{{ cls.__name__ }}<{{ scalar }}>;
{% endfor %}

// Print implementations
{% for scalar in scalar_types %}
std::ostream& operator<<(std::ostream& os, const geo::{{ cls.__name__ }}{{ scalar[0] }}& a) {
    const Eigen::IOFormat fmt(Eigen::StreamPrecision, Eigen::DontAlignCols, ", ", "\n", "[", "]");
    os << "<{{ cls.__name__ }}{{ scalar[0] }} " << a.Data().transpose().format(fmt) << ">";
    return os;
}
{% endfor %}

{# {% for scalar in scalar_types %}
extern template struct geo::{{ cls.__name__ | lower }}::StorageOps<{{ scalar }}>;
extern template struct geo::{{ cls.__name__ | lower }}::GroupOps<{{ scalar }}>;
extern template struct geo::{{ cls.__name__ | lower }}::LieGroupOps<{{ scalar }}>;
extern template struct geo::StorageOps<{{ scalar }}>;
extern template struct geo::GroupOps<{{ scalar }}>;
extern template struct geo::LieGroupOps<{{ scalar }}>;
{% endfor %} #}

// Concept implementations for this class
#include "./ops/{{ cls.__name__ | lower }}/storage_ops.cc"
#include "./ops/{{ cls.__name__ | lower }}/group_ops.cc"
{% if is_lie_group %}
#include "./ops/{{ cls.__name__ | lower }}/lie_group_ops.cc"
{% endif %}
