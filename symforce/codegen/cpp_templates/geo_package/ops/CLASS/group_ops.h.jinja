{%- import "../../../util/util.jinja" as util with context -%}

//  ----------------------------------------------------------------------------
// This file was autogenerated by symforce. Do NOT modify by hand.
// -----------------------------------------------------------------------------
#pragma once

#include <Eigen/Dense>

#include <geo/{{ camelcase_to_snakecase(cls.__name__) }}.h>

namespace sym {
namespace {{ camelcase_to_snakecase(cls.__name__) }} {

/**
 * C++ GroupOps implementation for {{ cls }}.
 */
template <typename Scalar>
struct GroupOps {
  using SelfJacobian = Eigen::Matrix<Scalar,
                                     sym::LieGroupOps<{{ cls.__name__ }}<Scalar>>::TangentDim(),
                                     sym::LieGroupOps<{{ cls.__name__ }}<Scalar>>::TangentDim()>;

  {% for spec in specs['GroupOps'] %}
  static {{ util.function_declaration(spec, is_declaration=True) }};
  {% endfor %}
};

}  // namespace {{ camelcase_to_snakecase(cls.__name__) }}

// Wrapper to specialize the public concept
{% for Scalar in scalar_types %}
template <>
struct GroupOps<{{ cls.__name__ }}<{{ Scalar }}>> : public {{ camelcase_to_snakecase(cls.__name__) }}::GroupOps<{{ Scalar }}> {};
{% endfor %}

}  // namespace sym
