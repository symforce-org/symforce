# -----------------------------------------------------------------------------
# This file was autogenerated by symforce. Do NOT modify by hand.
# -----------------------------------------------------------------------------

import numpy

{% set info = spec.types[typename] -%}
{% set np_scalar_type = np_scalar_types[spec.scalar_type] -%}

{% for subtype in info['subtypes'].values() -%}
from .{{ subtype }} import {{ subtype }}
{% endfor %}


class {{ typename }}:
    __slots__ = [
        {%+ for key in info['index'].keys() -%}
        '{{ key }}'{{ ',' if not loop.last }}
        {%+ endfor -%}
    ]

    def __init__(self):
        {% for key, (inx, datatype, shape, extra) in info['index'].items() %}
        {% if datatype == 'Values' %}
        self.{{ key }} = {{ info['subtypes'][key] }}()
        {% elif datatype == 'Scalar' %}
        self.{{ key }} = {{ np_scalar_type }}()
        {% else %}
        self.{{ key }} = numpy.zeros(({{ info['storage_dims'][key] }},), dtype={{ np_scalar_type }})
        {% endif %}
        {% endfor %}

        {%- if not info['index'] %}
        pass
        {% endif %}

    def __repr__(self):
        s = '<{{ typename }}'
        {% for key in info['index'].keys() %}
        s += '\n  {{ key }}={}'.format(self.{{ key }})
        {% endfor %}
        s += '>'
        return s
