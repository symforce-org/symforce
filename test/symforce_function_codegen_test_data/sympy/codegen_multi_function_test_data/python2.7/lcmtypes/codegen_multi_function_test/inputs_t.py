"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import codegen_multi_function_test.inputs_constants_t

import codegen_multi_function_test.inputs_states_t

import codegen_multi_function_test.values_vec_t


class inputs_t(object):
    __slots__ = [
        "x",
        "y",
        "rot",
        "rot_vec",
        "scalar_vec",
        "list_of_lists",
        "values_vec",
        "values_vec_2D",
        "constants",
        "states",
    ]

    __typenames__ = [
        "double",
        "double",
        "double",
        "double",
        "double",
        "double",
        "codegen_multi_function_test.values_vec_t",
        "codegen_multi_function_test.values_vec_t",
        "codegen_multi_function_test.inputs_constants_t",
        "codegen_multi_function_test.inputs_states_t",
    ]

    __dimensions__ = [None, None, [4], [3, 4], [3], [3, 3, 4], [3], [2, 1], None, None]

    def __init__(self):
        self.x = 0.0
        self.y = 0.0
        self.rot = [0.0 for dim0 in range(4)]
        self.rot_vec = [[0.0 for dim1 in range(4)] for dim0 in range(3)]
        self.scalar_vec = [0.0 for dim0 in range(3)]
        self.list_of_lists = [
            [[0.0 for dim2 in range(4)] for dim1 in range(3)] for dim0 in range(3)
        ]
        self.values_vec = [codegen_multi_function_test.values_vec_t() for dim0 in range(3)]
        self.values_vec_2D = [
            [codegen_multi_function_test.values_vec_t() for dim1 in range(1)] for dim0 in range(2)
        ]
        self.constants = codegen_multi_function_test.inputs_constants_t()
        self.states = codegen_multi_function_test.inputs_states_t()

    def encode(self):
        buf = BytesIO()
        buf.write(inputs_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">dd", self.x, self.y))
        buf.write(struct.pack(">4d", *self.rot[:4]))
        for i0 in range(3):
            buf.write(struct.pack(">4d", *self.rot_vec[i0][:4]))
        buf.write(struct.pack(">3d", *self.scalar_vec[:3]))
        for i0 in range(3):
            for i1 in range(3):
                buf.write(struct.pack(">4d", *self.list_of_lists[i0][i1][:4]))
        for i0 in range(3):
            assert (
                self.values_vec[i0]._get_packed_fingerprint()
                == codegen_multi_function_test.values_vec_t._get_packed_fingerprint()
            )
            self.values_vec[i0]._encode_one(buf)
        for i0 in range(2):
            for i1 in range(1):
                assert (
                    self.values_vec_2D[i0][i1]._get_packed_fingerprint()
                    == codegen_multi_function_test.values_vec_t._get_packed_fingerprint()
                )
                self.values_vec_2D[i0][i1]._encode_one(buf)
        assert (
            self.constants._get_packed_fingerprint()
            == codegen_multi_function_test.inputs_constants_t._get_packed_fingerprint()
        )
        self.constants._encode_one(buf)
        assert (
            self.states._get_packed_fingerprint()
            == codegen_multi_function_test.inputs_states_t._get_packed_fingerprint()
        )
        self.states._encode_one(buf)

    def decode(data):
        if hasattr(data, "read"):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != inputs_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return inputs_t._decode_one(buf)

    decode = staticmethod(decode)

    def _decode_one(buf):
        self = inputs_t()
        self.x, self.y = struct.unpack(">dd", buf.read(16))
        self.rot = struct.unpack(">4d", buf.read(32))
        self.rot_vec = []
        for i0 in range(3):
            self.rot_vec.append(struct.unpack(">4d", buf.read(32)))
        self.scalar_vec = struct.unpack(">3d", buf.read(24))
        self.list_of_lists = []
        for i0 in range(3):
            self.list_of_lists.append([])
            for i1 in range(3):
                self.list_of_lists[i0].append(struct.unpack(">4d", buf.read(32)))
        self.values_vec = []
        for i0 in range(3):
            self.values_vec.append(codegen_multi_function_test.values_vec_t._decode_one(buf))
        self.values_vec_2D = []
        for i0 in range(2):
            self.values_vec_2D.append([])
            for i1 in range(1):
                self.values_vec_2D[i0].append(
                    codegen_multi_function_test.values_vec_t._decode_one(buf)
                )
        self.constants = codegen_multi_function_test.inputs_constants_t._decode_one(buf)
        self.states = codegen_multi_function_test.inputs_states_t._decode_one(buf)
        return self

    _decode_one = staticmethod(_decode_one)

    _hash = None

    def _get_hash_recursive(parents):
        if inputs_t in parents:
            return 0
        newparents = parents + [inputs_t]
        tmphash = (
            0x22AA063EC20A5A12
            + codegen_multi_function_test.values_vec_t._get_hash_recursive(newparents)
            + codegen_multi_function_test.values_vec_t._get_hash_recursive(newparents)
            + codegen_multi_function_test.inputs_constants_t._get_hash_recursive(newparents)
            + codegen_multi_function_test.inputs_states_t._get_hash_recursive(newparents)
        ) & 0xFFFFFFFFFFFFFFFF
        tmphash = (((tmphash << 1) & 0xFFFFFFFFFFFFFFFF) + (tmphash >> 63)) & 0xFFFFFFFFFFFFFFFF
        return tmphash

    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if inputs_t._packed_fingerprint is None:
            inputs_t._packed_fingerprint = struct.pack(">Q", inputs_t._get_hash_recursive([]))
        return inputs_t._packed_fingerprint

    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)
