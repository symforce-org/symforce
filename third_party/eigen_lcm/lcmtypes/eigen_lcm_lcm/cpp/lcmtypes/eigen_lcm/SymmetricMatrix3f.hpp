// This file was autogenerated
// DO NOT MODIFY BY HAND!
#pragma once
#include <utility>

#include <Eigen/Dense>
#include <lcm/lcm_coretypes.h>

namespace eigen_lcm {

class SymmetricMatrix3f : public Eigen::Matrix<float, 3, 3, Eigen::DontAlign> {
 public:
  // Pass through constructor
  template <typename... Args>
  SymmetricMatrix3f(Args&&... args) : Eigen::Matrix<float, 3, 3, Eigen::DontAlign>(args...) {}

  // This method allows you to assign Eigen expressions to eigen_lcm::SymmetricMatrix3f
  template <typename OtherDerived>
  eigen_lcm::SymmetricMatrix3f& operator=(const Eigen::MatrixBase<OtherDerived>& other) {
    Eigen::Matrix<float, 3, 3, Eigen::DontAlign>::operator=(other);
    return *this;
  }

 private:
  // Disable comma initialization magic for eigen_lcm types
  template <typename T>
  Eigen::Matrix<float, 3, 3, Eigen::DontAlign>& operator<<(T other);

 public:
  /**
   * Encode a message into binary form.
   *
   * @param buf The output buffer.
   * @param offset Encoding starts at thie byte offset into @p buf.
   * @param maxlen Maximum number of bytes to write.  This should generally be
   *  equal to getEncodedSize().
   * @return The number of bytes encoded, or <0 on error.
   */
  inline int encode(void* buf, int offset, int maxlen) const;

  /**
   * Check how many bytes are required to encode this message.
   */
  inline int getEncodedSize() const;

  /**
   * Decode a message from binary form into this instance.
   *
   * @param buf The buffer containing the encoded message.
   * @param offset The byte offset into @p buf where the encoded message starts.
   * @param maxlen The maximum number of bytes to reqad while decoding.
   * @return The number of bytes decoded, or <0 if an error occured.
   */
  inline int decode(const void* buf, int offset, int maxlen);

  /**
   * Retrieve the 64-bit fingerprint identifying the structure of the message.
   * Note that the fingerprint is the same for all instances of the same
   * message type, and is a fingerprint on the message type definition, not on
   * the message contents.
   */
  constexpr static int64_t getHash();

  /**
   * Returns "SymmetricMatrix3f"
   */
  inline static const char* getTypeName();

  /**
   * Returns "eigen_lcm"
   */
  inline static const char* getPackageName();

  // LCM support functions. Users should not call these
  inline int _encodeNoHash(void* buf, int offset, int maxlen) const;
  inline int _getEncodedSizeNoHash() const;
  inline int _decodeNoHash(const void* buf, int offset, int maxlen);
  constexpr static uint64_t _computeHash(const __lcm_hash_ptr* p);
};

int SymmetricMatrix3f::encode(void* buf, int offset, int maxlen) const {
  int pos = 0, tlen;
  int64_t hash = (int64_t)getHash();

  tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
  if (tlen < 0)
    return tlen;
  else
    pos += tlen;

  tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
  if (tlen < 0)
    return tlen;
  else
    pos += tlen;

  return pos;
}

int SymmetricMatrix3f::decode(const void* buf, int offset, int maxlen) {
  int pos = 0, thislen;

  int64_t msg_hash;
  thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
  if (thislen < 0)
    return thislen;
  else
    pos += thislen;
  if (msg_hash != getHash())
    return -1;

  thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
  if (thislen < 0)
    return thislen;
  else
    pos += thislen;

  return pos;
}

int SymmetricMatrix3f::getEncodedSize() const {
  return 8 + _getEncodedSizeNoHash();
}

constexpr int64_t SymmetricMatrix3f::getHash() {
  return _computeHash(NULL);
}

const char* SymmetricMatrix3f::getTypeName() {
  return "SymmetricMatrix3f";
}

const char* SymmetricMatrix3f::getPackageName() {
  return "eigen_lcm";
}

int SymmetricMatrix3f::_encodeNoHash(void* buf, int offset, int maxlen) const {
  int pos = 0, tlen;

  Eigen::Matrix<float, 6, 1> flattened_lower_triangle;
  // Pack the lower triangle of the symmetric matrix into a vector
  flattened_lower_triangle.segment<3>(0) = this->block<3, 1>(0, 0);
  flattened_lower_triangle.segment<2>(3) = this->block<2, 1>(1, 1);
  flattened_lower_triangle.segment<1>(5) = this->block<1, 1>(2, 2);

  tlen = __float_encode_array(buf, offset + pos, maxlen - pos, flattened_lower_triangle.data(), 6);
  if (tlen < 0)
    return tlen;
  else
    pos += tlen;

  return pos;
}

int SymmetricMatrix3f::_decodeNoHash(const void* buf, int offset, int maxlen) {
  int pos = 0, tlen;

  Eigen::Matrix<float, 6, 1> flattened_lower_triangle;
  tlen = __float_decode_array(buf, offset + pos, maxlen - pos, flattened_lower_triangle.data(), 6);
  if (tlen < 0)
    return tlen;
  else
    pos += tlen;

  // Unpack the lower triangle of the symmetric matrix from the vector
  this->block<3, 1>(0, 0) = flattened_lower_triangle.segment<3>(0);
  this->block<2, 1>(1, 1) = flattened_lower_triangle.segment<2>(3);
  this->block<1, 1>(2, 2) = flattened_lower_triangle.segment<1>(5);
  this->triangularView<Eigen::StrictlyUpper>() =
      this->triangularView<Eigen::StrictlyLower>().transpose();

  return pos;
}

int SymmetricMatrix3f::_getEncodedSizeNoHash() const {
  int enc_size = 0;
  enc_size += __float_encoded_array_size(NULL, 6);
  return enc_size;
}

constexpr uint64_t SymmetricMatrix3f::_computeHash(const __lcm_hash_ptr*) {
  uint64_t hash = 0x5c782a36eaf5245cLL;
  return (hash << 1) + ((hash >> 63) & 1);
}

}  // namespace eigen_lcm
